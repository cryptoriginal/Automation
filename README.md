# mexc-webhook

TradingView -> Render webhook that places MEXC Futures orders.

- Put your MEXC API key, secret and ALERT_SECRET in the Render environment variables.
- Start in DRY_RUN=true to test.
- Verify ORDERS_PATH and body keys against MEXC contract API docs before going live.
